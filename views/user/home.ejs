<nav class="navbar">
  <div class="logo">
    <h3>CONFESS</h3>
  </div>
  <ul class="navlist">
    <li class="nav-item">
      <a href="/user/submit" class="nav-link signup">Submit</a>
    </li>
    <li class="nav-item"><a href="/user/logout" class="nav-link">Logout</a></li>
  </ul>
</nav>

<!----------------------------------------------------------------------------------->

<div class="content-wrapper">
  <% confessions.reverse().forEach((item)=>{ %>
  <!---->
  <% if(item.level === 'admin') {%>
  <div class="card">
    <div class="title">
      <h1><%=item.title%></h1>
      <p class="date"><%= item.date.toLocaleDateString() %></p>
    </div>

    <div class="content">
      <p><%=item.confession%></p>
    </div>
  </div>

  <% }}) %>
  <!-------------------------------------------------------------->
  <% confessions.forEach((item)=>{ %> <% if(item.level === 'user') {%>
  <div class="card">
    <div class="title">
      <h1><%=item.title%></h1>
      <p class="date"><%= item.date.toLocaleDateString() %></p>
    </div>

    <div class="content">
      <p><%=item.confession%></p>
    </div>
  </div>
  <% }}) %>
</div>
